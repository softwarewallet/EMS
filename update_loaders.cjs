const fs = require('fs');
const glob = require('glob');
const path = require('path');

// Assuming you have a standard glob or just use find.
// I'll use a simpler script using child_process and find.
